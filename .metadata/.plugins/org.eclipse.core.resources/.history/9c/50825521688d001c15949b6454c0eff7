package controller;

import javax.xml.validation.Validator;

public class ChangePwdCommandValidator extends Validator {

		@Override
		public boolean supports(Class<?> clazz) {
			return ChangePwdCommand.class.isAssignableFrom(clazz);
		}
}
